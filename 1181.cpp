#include <vector>
#include <string>
#include <algorithm>
using namespace std;

int main() {
	int n;
	string text;
	vector<string> v;
	cin >> n;
	for(int i = 0; i < n; i++) {
		cin >> text;
		v.push_back(text);
	}
	
	
}